import usePublications from '../hooks/usePublicatios';

const Publication = () => {
  const {}: any = usePublications();

  return (
    <>
      <div>Publication</div>
    </>
  );
};

export default Publication;
